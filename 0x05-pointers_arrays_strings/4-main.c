#include "main.h"

/**
 * main - checks the code
 *
 * Return: Always 0
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
